spring:
  profiles:
    active: dev

server:
  port: 8080

app:
  sms:
    api-key: ${BULKSMSBD_API_KEY:gp2fwmDalqLiOLqaK3QM}
    sender-id: ${BULKSMSBD_SENDER_ID:8809615555201}
  phone-verification:
    otp-expiry-minutes: 2
    resend-cooldown-seconds: 60

jwt:
  secret: YourSuperSecretKeyForJWTTokenGenerationMustBeLongEnough2024!
  expiration: 86400000

file:
  upload-dir: ./uploads

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:}
  api-key: ${CLOUDINARY_API_KEY:}
  api-secret: ${CLOUDINARY_API_SECRET:}

google:
  oauth2:
    # Web OAuth client ID used as the audience for Google ID tokens from the Flutter app.
    client-id: ${GOOGLE_OAUTH_CLIENT_ID:111634412431-th7l3d7cqtmrpqhrn16hvs55j2f6f9qa.apps.googleusercontent.com}

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    root: INFO
    com.cou.bustracker: DEBUG
    org.springframework.security: DEBUG
